export interface ContextType {
    author: string;
    title: string;
    publisher: string;
    year: string;
    place: string;
}

export interface ReferenceInfo {
    title: string;
    oldid: string;
}

console.log('Types loaded');